{% extends 'base.html.twig' %}

{% block body %}
    <div class="col-sm-3">
        {{ render(controller('AppBundle:KoulisHub:profile', { 'username':username })) }}
    </div>

    <div class="col-sm-9">
        {% include '/koulishub/repos.html.twig' %}
    </div>
{% endblock body %}